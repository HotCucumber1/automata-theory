int n = arr.size();
bool swapped;
int hihi = 123;

for (int i = 0; i < n - 1; ++i)
{
    swapped = false;

    for (int j = 0; j < n - i - 1; ++j)
    {
        if (arr[j] > arr[j + 1])
        {
            algs.swap(arr[j], arr[j + 1]);
            swapped = true;
        }
    }

    if (not swapped)
    {
        break;
    }
}
